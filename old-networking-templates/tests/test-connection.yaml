apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "networking.fullname" . }}-test-connection"
  labels:
    {{- include "networking.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "networking.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
