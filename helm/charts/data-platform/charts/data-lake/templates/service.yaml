apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-lake.fullname" . }}
  labels:
    {{- include "data-lake.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-lake.selectorLabels" . | nindent 4 }}
