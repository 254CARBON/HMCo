apiVersion: v1
kind: Service
metadata:
  name: {{ include "iceberg-rest-catalog.fullname" . }}
  labels:
    {{- include "iceberg-rest-catalog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "iceberg-rest-catalog.selectorLabels" . | nindent 4 }}
