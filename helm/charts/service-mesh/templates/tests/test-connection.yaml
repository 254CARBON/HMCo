apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service-mesh.fullname" . }}-test-connection"
  labels:
    {{- include "service-mesh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service-mesh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
