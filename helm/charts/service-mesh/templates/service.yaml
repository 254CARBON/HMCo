apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-mesh.fullname" . }}
  labels:
    {{- include "service-mesh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service-mesh.selectorLabels" . | nindent 4 }}
