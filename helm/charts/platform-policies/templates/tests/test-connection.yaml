apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "platform-policies.fullname" . }}-test-connection"
  labels:
    {{- include "platform-policies.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "platform-policies.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
