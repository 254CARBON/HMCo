# DataHub Helm Chart Values - With MySQL
# Reference: https://artifacthub.io/packages/helm/datahub/datahub

global:
  # Use existing Kafka
  kafka:
    bootstrap:
      server: "kafka-service:9092"
    schemaregistry:
      url: "http://schema-registry-service:8081"
  
  # Use existing Elasticsearch
  elasticsearch:
    host: "elasticsearch-service"
    port: "9200"
    useSSL: "false"
  
  # Use existing Neo4j
  neo4j:
    host: "graphdb-service:7474"
    uri: "bolt://graphdb-service:7687"
    username: "neo4j"
    password:
      value: "datahub_password"

# Enable MySQL (Helm chart will deploy it)
mysql:
  enabled: true

# Disable other database options
postgresql:
  enabled: false

# Disable Kafka, Elasticsearch, Neo4j (using existing services)
elasticsearch:
  enabled: false

neo4j:
  enabled: false

cp-helm-charts:
  enabled: false

kafka:
  enabled: false

# GMS Configuration
datahub-gms:
  enabled: true
  replicaCount: 1
  
  resources:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      cpu: "1"
      memory: 2Gi

# Frontend Configuration  
datahub-frontend:
  enabled: true
  replicaCount: 1

# Consumers
datahub-mae-consumer:
  enabled: true

datahub-mce-consumer:
  enabled: true

